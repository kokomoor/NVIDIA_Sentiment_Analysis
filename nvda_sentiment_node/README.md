# NVIDIA Sentiment Node

Python package for the NVDA sentiment node. Install with:

```bash
pip install -e .
# or, with FinBERT:
pip install -e '.[finbert]'
```

Full documentation — overview, architecture, and technical deep-dive —
is in the repository root: [`../README.md`](../README.md).
